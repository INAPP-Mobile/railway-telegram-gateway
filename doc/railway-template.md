# Railway Telegram Gateway - Template Reference\n\n## Railway.toml (Project Config)\n```tomln[build]\nbuilder = \"DOCKERFILE\"\ndockerfile = \"/Dockerfile\"\n\n[deploy]\nhealthcheckPath \"/health\"\nhealthcheckTimeout = 30\n\n# Service-specific settings\n[[services]]\nname = \"webhook-gateway\"\nbuilder = \"dockerfile\"\ndockerfile = \"/Dockerfile\"\nnames = [\"/webhook/*\", \"/api/*\", \"/ws/*\"]\n\n## Required Railway Env Vars\n```\nGATEWAY_API_KEY=<your-generated-api-key>\nWEBSOCKET_HEARTBEAT_INTERVAL=30000\nPORT=3000\nWEBHOOK_SECRET=optional-secret-for-verification\n```\n\n# Production Test Plan\n\nc## 1. Post-Deploy Smoke Tests\n| Step | Check | Expected |\n|------|-------|---------- |\n| Service health | GET /health | { \"status\": \"healthy\" } |\n| Webhook route exists | POST /webhook/bot/test (invalid) | 404 or valid response |\n| WS connection | Connect ws://host:port?token=<KEY> | Connection opens, handshake successful |\n\n## 2. Functional Tests\n### Bot Registration Flow\n1. ````POST /api/bots``\n```json {\n  \"name\": \"test-bot\",\n  \"shouldReconnect\": true\n}\n```\n→ { \"status\": \"connected\", subscriptions: [\"bot-test\"] }\n```json {\n  \"token\": \"<valid-tg-bot-token>\",\n  \"name\": \"integration-test\"\n}\n``\n→ { \"status\": \"registered\", botId: \"...\" }\n\n### Webhook Registration Verification\n1. Verify Telegram's `setWebhook` API was called with correct URL\n2. Confirm webhook endpoint responds with HTTP 200 on update delivery\n\n## 3. WebSocket Subscription Test```\ncurl -N \"ws://<host>:port?token=<API_KEY>\" \\\n  --header 'Sec-WebSocket-Protocol: gateway-v1'\n```\nExpected sequence:\n1. Connect → 200 connection open\n2. Send `{ \"type\": \"subscribe\", \"botId\": \"bot-test\" }\n3. Receive subscription confirmation `{ type: \"subscribed\" }`\n4. Deliver webhook message to the bot\n5. Confirm client receives `{ type: \"update\", payload: ... }`\n\n# 4. Error Boundary Tests```\n| Scenario | Expected |\n|----------|----------\n| Invalid token on WS connect | Close or reject handshake |\n| Unauthorized /api/bots request | HTTP 401 + error JSON |\n| Bot deregistration | Verify setWebhook delete in Telegram |\n| Multiple concurrent subscriptions | Each client receives updates independently |\n\n# Notes for Michael```\nc# Deployment checklist before pushing to Railway:\n```bash - [x] git remote add origin https://github.com/<your-org>/railway-telegram-gateway.git - [x] Set GATEWAY_API_KEY in Railway env vars- [x] Update BASE_URL or set it dynamically based on Railway service URL\n- [ ] Confirm Docker build succeeds```\n"